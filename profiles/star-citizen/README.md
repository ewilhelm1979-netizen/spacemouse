# Star Citizen profile

The sanitized delta profile is deliberately not committed yet. It will be
added only after a separate manual import test confirms all six axes, vertical
direction, ground-vehicle bindings, absence of drift and cross-axis input, and
absence of the inherited notification keyboard rebinds.

Do not publish the game's global action-map file; it is not a portable
controller profile.
